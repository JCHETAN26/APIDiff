# infra/terraform

GCP provisioning for APIDiff (Phase 8): project, GKE cluster, Cloud SQL
(Postgres), networking, IAM, and Secret Manager.

Kept intentionally empty in Phase 0 — the CI skeleton validates formatting here
once modules land.
